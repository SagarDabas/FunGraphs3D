<UserControl x:Class="FunGraphs3D.DeleteButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FunGraphs3D"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
       
    </UserControl.Resources>
    <Grid>
        <local:ArcButton x:Name="areaChart" Style="{StaticResource charts}" Opacity="1" ArcThickness="50"
                         FontSize="40" Stroke="#73A2E8" StrokeThickness="10" Foreground="#73A2E8" AllowDrop="True" Drop="areaChart_Drop_1">
            Delete
        </local:ArcButton>
    </Grid>
</UserControl>
