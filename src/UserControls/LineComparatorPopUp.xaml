<UserControl x:Class="FunGraphs3D.LineComparatorPopUp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Background="Gray" Opacity="0.3" MouseDown="Grid_MouseDown_1">
        <Popup x:Name="LinePopUp" IsOpen="True" Placement="Relative" Width="{Binding ActualWidth,RelativeSource={RelativeSource 
               Mode=FindAncestor, AncestorType=Grid}}" Height="{Binding ActualHeight,RelativeSource={RelativeSource
               Mode=FindAncestor, AncestorType=Grid}}" AllowsTransparency="True">
            <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto">
                <Grid x:Name="LinePopUpGrid" Opacity="0.9" />
            </ScrollViewer>
        </Popup>
    </Grid>
</UserControl>
